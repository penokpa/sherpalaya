; Overrides applied to `php artisan serve` via PHP_INI_SCAN_DIR.
; Production (nginx + PHP-FPM) ignores this file — set the same limits in your FPM pool.

upload_max_filesize = 15M
post_max_size = 20M
memory_limit = 256M
